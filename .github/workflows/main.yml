name: Build PMM docs ready for publishing

on:
  push:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
        
      - name: Install Docker
        run: |
          sudo apt install -y docker
          docker --version
