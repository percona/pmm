name: Test podman.md

on:
  push:
    branches:
      - v3
  pull_request:
    paths:
      - 'documentation/docs/install-pmm/install-pmm-server/baremetal/podman/index.md'

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: Run podman tests
        run: ./documentation/resources/bin/test-podman.sh

      - name: Get debug
        if: ${{ failure() }}
        run: |
          podman version
          podman ps --all
          systemctl --user status pmm-server.service || true
          podman inspect pmm-server || true
          journalctl --user -xe
