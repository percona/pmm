---
name: Linkspector
on:
  pull_request:
    paths:
      - "documentation/**"
      - "!documentation/api/**"
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref (branch, tag, or SHA) to check links in."
        required: false
        default: ""

permissions:
  contents: read

jobs:
  check-links:
    name: linkspector
    runs-on: ubuntu-latest
    if: ${{ github.actor != 'dependabot[bot]' }}
    steps:
      - name: Check out code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Install Chrome for linkspector
        id: setup-chrome
        uses: browser-actions/setup-chrome@48ad923757ca74d66703209fe939badbdf80f2f4 # v2.2.0

      - name: Configure Chrome path for puppeteer
        run: |
          CHROME_WRAPPER="${{ runner.temp }}/linkspector-chrome.sh"
          cat > "${CHROME_WRAPPER}" <<'EOF'
          #!/usr/bin/env bash
          exec "${{ steps.setup-chrome.outputs.chrome-path }}" --no-sandbox "$@"
          EOF
          chmod +x "${CHROME_WRAPPER}"
          echo "PUPPETEER_EXECUTABLE_PATH=${CHROME_WRAPPER}" >> "$GITHUB_ENV"

      - name: Run linkspector
        uses: umbrelladocs/action-linkspector@568ec8d29fa92b31fd9ea5381e155c51e922af83 # v1.5.5
        with:
          github_token: ${{ secrets.ROBOT_TOKEN || secrets.GITHUB_TOKEN }}
          reporter: ${{ github.event_name == 'pull_request' && 'github-pr-review' || 'github-check' }}
          fail_level: error
          filter_mode: nofilter
          config_file: documentation/.linkspector.yml
