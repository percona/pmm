{
  "swagger": "2.0",
  "info": {
    "title": "platformpb/platform.proto",
    "version": "version not set"
  },
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/v0/Platform/Connect": {
      "post": {
        "summary": "Connect connects a PMM server to the organization created on Percona Portal. That allows the user to sign in to the PMM server with their Percona Account.",
        "operationId": "Connect",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/platformConnectResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/runtimeError"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/platformConnectRequest"
            }
          }
        ],
        "tags": [
          "Platform"
        ]
      }
    },
    "/v0/Platform/SignUp": {
      "post": {
        "summary": "SignUp creates a new Percona Platform user.",
        "operationId": "SignUp",
        "responses": {
          "200": {
            "description": "A successful response.",
            "schema": {
              "$ref": "#/definitions/platformSignUpResponse"
            }
          },
          "default": {
            "description": "An unexpected error response.",
            "schema": {
              "$ref": "#/definitions/runtimeError"
            }
          }
        },
        "parameters": [
          {
            "name": "body",
            "in": "body",
            "required": true,
            "schema": {
              "$ref": "#/definitions/platformSignUpRequest"
            }
          }
        ],
        "tags": [
          "Platform"
        ]
      }
    }
  },
  "definitions": {
    "platformConnectRequest": {
      "type": "object",
      "properties": {
        "server_name": {
          "type": "string",
          "description": "User defined human readable PMM Server Name."
        },
        "email": {
          "type": "string",
          "description": "Existing Percona Platform user's email."
        },
        "password": {
          "type": "string",
          "description": "Existing Percona Platform user's password."
        }
      }
    },
    "platformConnectResponse": {
      "type": "object"
    },
    "platformSignUpRequest": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "description": "New Percona Platform user's email."
        },
        "password": {
          "type": "string",
          "description": "Existing Percona Platform user's password."
        },
        "first_name": {
          "type": "string",
          "description": "New Percona Platform user's first name."
        },
        "last_name": {
          "type": "string",
          "description": "New Percona Platform user's last name."
        }
      }
    },
    "platformSignUpResponse": {
      "type": "object"
    },
    "protobufAny": {
      "type": "object",
      "properties": {
        "type_url": {
          "type": "string"
        },
        "value": {
          "type": "string",
          "format": "byte"
        }
      }
    },
    "runtimeError": {
      "type": "object",
      "properties": {
        "error": {
          "type": "string"
        },
        "code": {
          "type": "integer",
          "format": "int32"
        },
        "message": {
          "type": "string"
        },
        "details": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/protobufAny"
          }
        }
      }
    }
  }
}
