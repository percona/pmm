{
  "swagger": "2.0",
  "info": {
    "title": "demo.proto",
    "version": "version not set"
  },
  "schemes": [
    "http",
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/v0/ping": {
      "get": {
        "operationId": "Ping",
        "responses": {
          "200": {
            "description": "",
            "schema": {
              "$ref": "#/definitions/apiDemoPingResponse"
            }
          }
        },
        "parameters": [
          {
            "name": "type",
            "in": "query",
            "required": false,
            "type": "string",
            "enum": [
              "PING",
              "PONG"
            ],
            "default": "PING"
          },
          {
            "name": "cookie",
            "in": "query",
            "required": false,
            "type": "string",
            "format": "uint64"
          }
        ],
        "tags": [
          "Demo"
        ]
      }
    }
  },
  "definitions": {
    "apiDemoPingRequest": {
      "type": "object",
      "properties": {
        "type": {
          "$ref": "#/definitions/apiDemoPingType"
        },
        "cookie": {
          "type": "string",
          "format": "uint64"
        }
      }
    },
    "apiDemoPingResponse": {
      "type": "object",
      "properties": {
        "type": {
          "$ref": "#/definitions/apiDemoPingType"
        },
        "cookie": {
          "type": "string",
          "format": "uint64"
        }
      }
    },
    "apiDemoPingType": {
      "type": "string",
      "enum": [
        "PING",
        "PONG"
      ],
      "default": "PING"
    }
  }
}
