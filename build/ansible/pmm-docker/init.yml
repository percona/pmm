---
# This playbook is executed by the `pmm-init` supervisord process to initialize the PMM environment
- name: PMM Initialization Playbook
  hosts: all
  become: true
  become_method: su
  become_user: pmm
  gather_facts: true

  roles:
    - initialization
