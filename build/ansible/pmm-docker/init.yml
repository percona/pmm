---
# This playbook contains tasks executed during initialization PMM Server
- hosts: localhost
  become: true
  become_method: su
  become_user: pmm
  gather_facts: true


  roles:
    - initialization
