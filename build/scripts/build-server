#!/bin/bash


set -o errexit
set -o xtrace

. $(dirname $0)/vars

# build pmm-client
pmm_version=${pmm_version} ${bin_dir}/build-client

# build server components
${bin_dir}/build-server-binaries

# build pmm-server
${bin_dir}/build-server-docker

# vim: expandtab shiftwidth=4 tabstop=4
