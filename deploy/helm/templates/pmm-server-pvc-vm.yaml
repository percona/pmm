apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "pmm-headless.fullname" . }}-vm-data
  labels:
    {{- include "pmm-headless.labels" . | nindent 4 }}
    component: victoriametrics
spec:
  accessModes:
    - ReadWriteOnce
  {{- include "pmm-headless.storageClass" (dict "component" .Values.pmmServer.persistence.victoriametrics "global" .Values.global) | nindent 2 }}
  resources:
    requests:
      storage: {{ .Values.pmmServer.persistence.victoriametrics.size }}
