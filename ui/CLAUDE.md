<!--
Pointer file for Claude Code, which reads CLAUDE.md and not AGENTS.md.
The import resolves relative to this file, so it loads this directory's
AGENTS.md, and Claude Code loads it on demand when it first reads a file
here. Keep the import as the only instruction — guidance belongs in
AGENTS.md. This comment is stripped before the file enters context.
-->

@AGENTS.md
