dev:
	yarn dev

ci: setup format-check lint test build

format: setup
	yarn format

format-check: setup
	yarn format:check

lint: setup
	yarn lint

test: setup
	yarn test

setup:
	yarn install

build: setup
	yarn build

release: build

clean:
	yarn clean